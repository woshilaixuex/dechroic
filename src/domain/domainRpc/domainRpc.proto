syntax = "proto3";

package domainRpc;
option go_package="./domainRpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service DomainRpc {
  rpc Ping(Request) returns(Response);
}
